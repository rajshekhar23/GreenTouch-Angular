import { CharacterDirective } from './character.directive';

describe('CharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterDirective();
    expect(directive).toBeTruthy();
  });
});
